fastapi==0.115.0
uvicorn==0.30.6
python-multipart==0.0.9
watchfiles==0.24.0
redis==5.0.8
pydantic-settings==2.2.1
pypdf==5.1.0
cryptography==44.0.0  # нужна pypdf для расшифровки AES-зашифрованных PDF
pillow==12.1.0
music21==10.3.0
numpy==2.4.6
opencv-python-headless==4.13.0.92
verovio==6.2.1
# Фото-OMR для single-задач (JPEG/HEIC). Лёгкая ONNX-сборка homr (без torch);
# тянет onnxruntime, rapidocr-onnxruntime, musicxml. Требует Python 3.10–3.12.
homr==0.6.2
# HEIC/HEIF с iPhone: Pillow сам их не открывает (features.check('heif') == False),
# без этого пакета снимок с телефона не читается ни новым путём, ни старым.
pillow-heif==0.21.0
# Растеризация PDF для пакета omr/ (PDF идёт постранично, а не книгой целиком).
# Именно pypdfium2: разрешительная лицензия и самодостаточные wheel'ы, в отличие
# от pdf2image он не требует poppler в образе.
pypdfium2==4.30.0
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
sqladmin==0.20.1
fastapi-storages==0.3.0
itsdangerous==2.2.0
python-slugify==8.0.4
httpx==0.27.2
cairosvg==2.7.1
